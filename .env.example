# Copy this file to .env and fill in your token (optional but recommended).
# A GitHub personal access token raises the API rate limit from 60/hr to 5,000/hr.
# Create one at: https://github.com/settings/tokens (no scopes needed for public repos)
GITHUB_TOKEN=
